// Content collections are not used on this site — the /cv, /publications and
// /teaching pages import their YAML-only markdown files directly as frontmatter.
// This empty config silences Astro's "Content config not loaded" warning.
export const collections = {};
